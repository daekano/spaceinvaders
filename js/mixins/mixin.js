/**
 * Mixin
 * Space Invaders Package
 * Matthew Evans, 2015
 *
 * The Mixin is an object that allows for a certain interface.
 * For example, we can define the 'renderable' mixin that allows the actor to be picked up
 * and rendered because it is guaranteed to have the correct information.
 */

/**
 * Mixin Constructor
 */
function Mixin() {

}